import { type CreateFetchCacheOptions, type FetchCache, type Runtime } from "../fetch-cache.js";
export type { 
/** Options accepted by `createFetchCache`. */
CreateFetchCacheOptions, 
/** Cached fetch function returned by `createFetchCache`. */
FetchCache, 
/** Built-in cache behavior modes. */
FetchCacheMode, 
/** Per-call or global options that control cache behavior. */
FetchCacheOptions, 
/** Policy for deciding whether a cache lookup should run. */
ReadCacheOption, 
/** Runtime adapter contract used by stores. */
Runtime, 
/** Policy for deciding whether fetched content is stored. */
WriteCacheOption, } from "../fetch-cache.js";
/**
 * Runtime adapter that provides Deno environment, hashing, filesystem, path,
 * and cwd operations to stores.
 */
export declare const runtime: Runtime;
/**
 * Creates a cached `fetch` implementation using the Deno runtime adapter.
 *
 * @example
 * ```ts
 * import createFetchCache from "fetch-mock-cache/runtimes/deno.ts"
 * import Store from "fetch-mock-cache/stores/memory";
 * const fetchCache = createFetchCache({ Store });
 * ```
 */
export default function createFetchCache(options?: Partial<CreateFetchCacheOptions>): FetchCache;
